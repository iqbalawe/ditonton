name: movies
description: "A new Flutter package project."
version: 0.0.1

publish_to: none

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.4.1
  core:
    path: ../core
  dartz: ^0.10.1
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  http: ^1.6.0
  provider: ^6.1.5+1
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.6.3
  build_runner: ^2.10.5
