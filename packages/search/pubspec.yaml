name: search
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.10.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.6.3
